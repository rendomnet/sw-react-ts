// jest.config.js (or jest.config.ts)

module.exports = {
  // ... other configuration options ...

  // Point to the jest setup file
  setupFilesAfterEnv: ["./jest.setup.ts"],

  // ... other configuration options ...
};
